# 复制本文件为 .env 并填写；.env 已在 .gitignore 中，不会被提交。
# 命令行参数优先于此处的配置。

# 上海科技大学统一身份认证账号（学号@密码），启动时自动登录获取 GenAI token
GENAI_ACCOUNT=

# 直接配置 GenAI token（优先于自动登录与缓存 token）
GENAI_TOKEN=

# 图片上传接口的 token 请求头（可选，默认内置当前可用值）
GENAI_UPLOAD_TOKEN=
